version: "2.2"

services:
  og-api:
    build: .
    container_name: OgnareAPIRest
    command: npm run application
    restart: always
    ports:
      - 3030:3030
    volumes:
      - ./src:/usr/app/src
